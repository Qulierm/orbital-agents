/** Type surface for the agent-preset patch generator. */

export const PRESET_IDS: string[]
export function renderPresetPatch(root: string, id: string): string
export function writePresetPatches(root: string): string[]
